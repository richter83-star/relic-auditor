# Relic Auditor v1.0.1 reconciliation ledger

Status: integration work in progress. This document is not a release claim.

## Why this branch exists

The repository developed two disconnected histories. The validated v0.12 release line contains the Product Builder Bridge, licensing, approval-gated Supervisor/runtime, secure updater/trust roots, LLM health, Windows installer lifecycle, and their regression suites. The later `main` line contains useful Resurrection Mode and Technical Truth fixes but does not contain those v0.12 systems.

A normal merge is therefore not safe. This branch starts from the exact validated v0.12 commit and ports later work selectively.

## Authoritative base

- Branch: `installer/v0.12.0`
- Commit: `95284fc00a7f6daa3f224f97a5d387c07348259b`
- Prior Windows validation run: `31901610921`
- Prior verified unsigned installer SHA-256: `5d5c8747eff13a1f0360152f0c9e2faeb2691cacc322dc37bb0f8b19a6870229`

Those identifiers prove only the v0.12 baseline. They do not validate this reconciliation branch.

## Ported so far

- Resurrection deterministic substantive-subgraph extraction
- deterministic salvageability gate and forced `TOSS_IT` path
- bounded optional LLM reasoning and citation verification
- secret-redacted Resurrection reports
- market-context schema and offline heuristic provider
- correction that explicitly labels bundled market context as `offline_heuristic` / `external_market_speculation`; no live-market claim
- removal of the duplicate unreachable Resurrection exception handler
- Technical Truth bare-raise stub detection fix
- Technical Truth call-argument ordering fix
- Technical Truth Next.js App Router reconstruction fix
- Technical Truth route suffix collision hardening
- `relic resurrect <target>` compatibility entrypoint without replacing the existing v0.12 CLI
- Resurrection regression coverage adapted to the unified entrypoint

## Preserved from v0.12

- deterministic Build Packs
- entitlement boundaries
- licensing and OS credential storage
- approval-gated Supervisor/runtime
- workspace isolation and audit ledger
- updater and embedded trust roots
- LLM health checks
- dashboard integration
- Windows installer and update lifecycle machinery
- v0.12 regression suites

## Deliberately not claimed yet

- v1.0.1 release readiness
- a validated Windows installer for this branch
- Authenticode signing
- a live market-research adapter
- successful complete test-suite results for this branch
- successful frozen-source reproduction for this branch
- safe replacement of the repository default branch

## Remaining gates before v1.0.1 RC

1. Run the complete unified source test suite on Windows.
2. Fix any compatibility failures caused by combining the lineages.
3. Decide whether `audit --resurrection` should be restored in addition to the standalone command without weakening the v0.12 CLI contracts.
4. Harden citation grounding beyond core paths/evidence IDs to structured proposed filenames, symbols, remediation steps, and bridge components where applicable.
5. Bump version metadata coherently only after the unified source is green.
6. Regenerate deterministic frozen source and release artifacts from the unified tree.
7. Generalize the v0.12 Windows installer workflow for v1.0.1 and rerun CLI/GUI smoke, clean install, in-place upgrade, stale-runtime cleanup, config preservation, PATH cleanup, and uninstall.
8. Preserve the fail-closed updater behavior. Automatic install must remain blocked until the installer and stable manifest satisfy configured signing/trust requirements.
9. Rewrite README, build status, upgrade notes, and release notes to describe the unified architecture truthfully.
10. Reconcile the repository default branch only after review of an exact tree/commit and explicit authorization. Do not erase the old history accidentally.

## Publication boundary

No tag, GitHub Release, package-index upload, installer publication, or default-branch replacement is authorized by this integration work.
