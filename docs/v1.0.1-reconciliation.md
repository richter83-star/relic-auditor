# Relic Auditor v1.0.1 reconciliation ledger

Status: unified release candidate. This document is not a release or publication claim.

## Why this branch exists

The repository developed two disconnected histories. The validated v0.12 release line contains the Product Builder Bridge, licensing, approval-gated Supervisor/runtime, secure updater/trust roots, LLM health, Windows installer lifecycle, and their regression suites. The later `main` line contains useful Resurrection Mode and Technical Truth fixes but does not contain those v0.12 systems.

A normal merge is therefore not safe. The reconciliation branch starts from the exact validated v0.12 commit and ports later work selectively.

## Authoritative base

- Branch: `installer/v0.12.0`
- Commit: `95284fc00a7f6daa3f224f97a5d387c07348259b`
- Prior Windows validation run: `31901610921`
- Prior verified unsigned v0.12 installer SHA-256: `5d5c8747eff13a1f0360152f0c9e2faeb2691cacc322dc37bb0f8b19a6870229`

Those identifiers prove only the v0.12 baseline. They do not validate v1.0.1.

For PR check execution only, draft PR #4 is temporarily based on `reconcile/v0.12-validation-base`. That branch started from the exact authoritative v0.12 commit and contains only reconciliation validation-workflow changes. The validated `installer/v0.12.0` branch itself remains untouched.

## Ported and reconciled

- Resurrection deterministic substantive-subgraph extraction
- deterministic salvageability gate and forced `TOSS_IT` path
- bounded optional LLM reasoning using the v0.12 provider contract
- citation and path-reference grounding with deterministic fallback
- secret-redacted Resurrection JSON and Markdown reports
- market-context schema and explicitly offline heuristic provider
- removal of the duplicate unreachable Resurrection exception handler
- Technical Truth bare-raise stub detection fix
- Technical Truth call-argument ordering fix
- Technical Truth Next.js App Router reconstruction fix
- Technical Truth route suffix collision hardening
- `relic resurrect <target>` compatibility entrypoint without replacing existing v0.12 CLI commands
- frozen Windows CLI entrypoint routed through the same unified dispatcher
- Resurrection regression coverage, including provider-contract and grounding tests

## Preserved from v0.12

- deterministic Build Packs
- entitlement boundaries
- licensing and OS credential storage
- approval-gated Supervisor/runtime
- workspace isolation and audit ledger
- updater and embedded trust roots
- LLM health checks
- dashboard integration
- Windows installer and update lifecycle machinery
- v0.12 regression suites

## Validation history

### Unified source baseline

Commit `8a888578063d1734880eb1df49efb6fad04ba065` passed Windows workflow run `32449946694`:

- complete source test suite: passed
- unified CLI smoke: passed
- wheel build: passed
- sdist build: passed

### v1.0.1 metadata and packaging pass

Commit `c19df5391125932bb414dbf9ed7284dced0ece12` moved package, executable, manifest, Inno Setup, installer lifecycle, and installer-test metadata coherently to v1.0.1. Windows workflow run `32450238274` passed the source/package gate.

### Exact-head Windows lifecycle pass

Commit `f36791ac1df3e0f8e2068f883bb8907d2dc2bdf0` passed workflow run `32453060126` after two installer-source validation defects were exposed and fixed. The final fix uses a CRLF/LF-safe exact metadata-line check rather than a fragile multiline regex.

That exact run passed:

- complete source tests and unified CLI smoke
- wheel and sdist build
- frozen exact-commit source generation and hash verification
- frozen-source tests
- PyInstaller GUI and CLI construction
- bundled audit, acquisition, Resurrection, and GUI smoke
- clean install and installed CLI/GUI smoke
- PATH registration and pre-existing config preservation
- in-place upgrade and stale-runtime cleanup
- uninstall, config preservation, and PATH cleanup
- release manifest/checksum generation and artifact upload

For that exact commit:

- frozen source SHA-256: `2974e6f2c3b8485278327a23536d2b946435ab55d929d6bcc31d20c1c88a5479`
- installer SHA-256: `51223411a5de103ff9d2e5df1c746cefe85f7997cd9355c9ccfd68e217ceafe0`
- installer size: `73,927,403` bytes
- Authenticode: `NotSigned`
- workflow artifact ID: `9436396583`
- artifact ZIP SHA-256: `62f825e651243d1b213842117f74a53cd79be1344c44435ee26d94d86f7e1ab2`

These hashes apply only to commit `f36791ac...`. Any later source commit requires a newly frozen source archive and newly generated RC evidence.

## RC packaging rules

- frozen source is generated from the exact checked-out PR-head commit using `git archive`
- the frozen source SHA-256 is computed before installer construction and passed explicitly to the installer build script
- the build script refuses to proceed without an expected source SHA-256
- both the checked-out tree and frozen source test suites run before packaging
- bundled and installed CLI smoke include audit, acquisition, and Resurrection Mode
- GUI smoke, clean install, in-place upgrade, stale-runtime cleanup, user-config preservation, PATH cleanup, and uninstall are mandatory gates
- unsigned installers remain test artifacts; the updater remains fail-closed unless configured trust requirements are satisfied

## Deliberately not claimed

- Authenticode signing
- a live market-research adapter
- `audit --resurrection`; standalone `relic resurrect` is the supported RC interface
- safe replacement of the repository default branch
- a public v1.0.1 release or installer publication

## Remaining gates before publication

1. Re-run exact-head source and Windows lifecycle checks after the final documentation/source commit.
2. Review the final reconciliation diff and choose a safe default-branch strategy with explicit authorization.
3. Protect the authoritative branch / required checks before normal development resumes.
4. Provision production signing and stable update trust if automatic update installation will be enabled.
5. Obtain explicit authorization before merge, default-branch replacement, tag, GitHub Release, package-index upload, or public installer publication.

## Publication boundary

No tag, GitHub Release, package-index upload, installer publication, merge to `main`, or default-branch replacement is authorized by this integration work.
